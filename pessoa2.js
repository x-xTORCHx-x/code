let pessoa = {
    nome: 'John',
    idade: 40,
    profisao: 'General do Exercito Britanico'
};

function age(){
    console.log(`${pessoa.nome}, ${pessoa.profisao}, tem ${pessoa.idade}, ano que vem ele faz ${(pessoa.idade + 1)}`)
};

function ag(){
    pessoa.proflet pessoa = {
    nome: 'John',
    idade: 40,
    profisao: 'General do Exercito Britanico'
};

function age(){
    console.log(`${pessoa.nome}, ${pessoa.profisao}, tem ${pessoa.idade}, ano que vem ele faz ${(pessoa.idade + 1)}`)
};

function ag(){
    pessoa.profisao = 'medico';
    console.log(`${pessoa.nome}, ${pessoa.profisao}, tem ${pessoa.idade}, ano que vem ele faz ${(pessoa.idade + 1)}`)
};
console.log(age());
console.log(ag());isao = 'medico';
    console.log(`${pessoa.nome}, ${pessoa.profisao}, tem ${pessoa.idade}, ano que vem ele faz ${(pessoa.idade + 1)}`)
};
console.log(age());
console.log(ag());
